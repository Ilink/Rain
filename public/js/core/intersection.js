function sphereSphere(aRadius, bRadius, ax, ay, az, bx, by, bz){
	/*
	if the radiuses are the same:
		vector = b - a
		get vector length
		if length < radius, they collided

	if the radiuses are different..

	*/
	if(length < aRadius + bRadius)
		return true
	else 
		return false;
}

